<%= form_with(model: multimedia_type, local: true) do |form| %>
  <% if multimedia_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(multimedia_type.errors.count, "error") %> prohibited this multimedia_type from being saved:</h2>

      <ul>
      <% multimedia_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nam_multimedia_type %>
    <%= form.text_field :nam_multimedia_type %>
  </div>

  <div class="field">
    <%= form.label :desc_multimedia_type %>
    <%= form.text_field :desc_multimedia_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
