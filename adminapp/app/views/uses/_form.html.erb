<%= form_with(model: use, local: true) do |form| %>
  <% if use.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(use.errors.count, "error") %> prohibited this use from being saved:</h2>

      <ul>
      <% use.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nam_use %>
    <%= form.text_field :nam_use %>
  </div>

  <div class="field">
    <%= form.label :desc_ %>
    <%= form.text_field :desc_ %>
  </div>

  <div class="field">
    <%= form.label :use %>
    <%= form.text_field :use %>
  </div>

  <div class="field">
    <%= form.label :id_material %>
    <%= form.number_field :id_material %>
  </div>

  <div class="field">
    <%= form.label :id_user %>
    <%= form.number_field :id_user %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
